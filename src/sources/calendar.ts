// Deferred to Phase 3b/4 — requires Google Calendar MCP auth.
// Will implement SyncSource interface: poll() → gcal_list_events → RawDrops.
// Same contract as markpush.ts / gmail.ts with DI for MCP tools.
export {};
